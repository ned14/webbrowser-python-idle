# The pinned @playwright/test 1.48.0 deadlocks its ESM loader on Node >= 24
# (`playwright test` hangs forever with no output — see package.json engines).
# Fail the install with a clear message instead of hanging for hours.
engine-strict=true
