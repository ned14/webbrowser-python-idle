Pinned upstream commit of https://github.com/leaningtech/webvm.

Commit SHA: 8d68d2b18fa04d72ba49bc6c5b8c684a934fc268
Date:       2026-08-13
Message:    "Fix promo length"

Pinned dependencies (package.json / package-lock.json):
- @leaningtech/cheerpx: 1.3.8 (exact)
- labs: no longer a dependency of this tree — the Claude/AI sidebar tab (the
  only consumer of leaningtech/labs) was removed, so package.json/lock contain
  no labs entry (the upstream git@github.com:... entry is gone with it).

NOTE (2026-08-18, Tier A of plans/update-to-latest.md): only the CheerpX
1.3.7 -> 1.3.8 bump (upstream commits "Update CheerpX version to 1.3.8" +
the two promo-text edits to src/lib/messages.js) was taken from the upstream
range e58fef0c9..8d68d2b18. This repo's frontend is deeply diverged from
upstream (custom error overlay, session guard, baked config seeding,
network.js newIPN driver); upstream is NEVER re-vendored — the pin is
documentation only (the vendored tree stays this repo's modified frontend).

See plans/webvm_implementation.md §3/9, §12/8 for the pinning policy and the
§12/21 implementation-time verification checklist.
