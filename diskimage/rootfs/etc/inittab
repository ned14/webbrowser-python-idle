# /etc/inittab
#
# openrc runs through rc-preload so the CheerpX shims (LD_PRELOAD) are set
# for the whole boot chain — busybox init cannot inject environment
# variables. openrc 0.60+ calls faccessat(-1, ...) on every service state
# check, which traps the CheerpX emulator without the fix
# (diskimage/faccessat-fix.c, plans/update-to-latest.md Tier B §9.2).
# rc_env_allow="LD_PRELOAD" in /etc/rc.conf keeps the variable in the
# exec'd init scripts' environment.

::sysinit:/usr/local/sbin/rc-preload /sbin/openrc sysinit
::sysinit:/usr/local/sbin/rc-preload /sbin/openrc boot
::wait:/usr/local/sbin/rc-preload /sbin/openrc default

# No gettys: this is a single-user WebVM desktop with implicit autologin — the
# X session is started by /etc/local.d/desktop.start. Keeping gettys here would
# leave the console stuck on a "login:" prompt instead of booting the desktop.
#tty1::respawn:/sbin/getty 38400 tty1
#tty2::respawn:/sbin/getty 38400 tty2
#tty3::respawn:/sbin/getty 38400 tty3
#tty4::respawn:/sbin/getty 38400 tty4
#tty5::respawn:/sbin/getty 38400 tty5
#tty6::respawn:/sbin/getty 38400 tty6

# Put a getty on the serial port
#ttyS0::respawn:/sbin/getty -L ttyS0 115200 vt100

# Stuff to do for the 3-finger salute
::ctrlaltdel:/sbin/reboot

# Stuff to do before rebooting
::shutdown:/sbin/openrc shutdown
